{
  "name": "unlockit",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "docker": "docker compose --env-file .env.development -f docker-compose.dev.yml up --build",
    "docker:down": "docker compose --env-file .env.development -f docker-compose.dev.yml down -v",
    "docker:db": "docker compose --env-file .env.development -f docker-compose.dev.yml up --build db pgadmin",
    "docker:db:down": "docker compose --env-file .env.development -f docker-compose.dev.yml down -v db && docker compose --env-file .env.development -f docker-compose.dev.yml down pgadmin",
    "dev": "concurrently -k -p \"[{name}]\" -n \"SHARED,FRONT,BACK\" -c \"magenta,cyan,green\" \"npm run dev:shared\" \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:shared": "npm run dev --workspace=@unlockit/shared",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run start:dev --workspace=backend",
    "build": "npm run build:shared && concurrently \"npm run build:frontend\" \"npm run build:backend\"",
    "build:shared": "npm run build --workspace=@unlockit/shared",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "cross-env": "^10.1.0",
    "dotenv-cli": "^11.0.0"
  },
  "dependencies": {
    "@nestjs/schedule": "^6.1.3",
    "@nestjs/swagger": "^11.4.4"
  }
}
